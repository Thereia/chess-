# Project Short Card

## Current Task
Start Task 3: board initialization and helpers.

## Current State
- Task 2 code and tests are written and verified.
- Task 2 still needs to be committed if not already committed.
- Project path contains `!`; Maven tests should run from a no-`!` path or temporary `subst X:` mapping.

## Immediate Actions
1. Commit Task 2 files if still uncommitted.
2. Write failing tests for initial Jieqi board setup.
3. Implement `Board.initial()`, `Board.empty()`, lookup, put/remove/move, and path counting.
4. Run focused board tests and commit Task 3.

## Completion Criteria
- `BoardTest` passes.
- Task 3 is committed before moving to flip pool.
